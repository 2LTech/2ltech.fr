'use client'

import NotFound from '@/components/notFound'

import Framework from './framework'

/**
 * Not found
 * @returns NotFound
 */
const NotFoundPage = () => (
  <Framework>
    <NotFound />
  </Framework>
)

export default NotFoundPage
